@using PoundPupLegacy.ViewModel;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<Link>

@if (Model.Any())
{
    <h3>Professions</h3>
    <div class="person-types">
        @foreach (var link in Model)
        {
            <div><partial name="_Link" model="@link" /></div>
        }
    </div>
}
