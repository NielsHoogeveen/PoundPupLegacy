@using PoundPupLegacy.ViewModel;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model IEnumerable<Link>

@if (Model.Any())
{
    <ul>
        @foreach (var link in Model)
        {
            <li><partial name="_Link" model="@link" /></li>
        }
    </ul>
}
