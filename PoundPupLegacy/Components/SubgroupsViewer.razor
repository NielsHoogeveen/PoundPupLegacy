@using PoundPupLegacy.Models
@using PoundPupLegacy.Services
@inject ISubgroupsService SubgroupsService
@inherits ViewerBase
<BreadCrumElementViewer Model="BreadCrumElements" />
<h1>Groups</h1>
@if(_model is not null) {
    <div class="create-options">
    @foreach(var elem in _model) {
        <div><a href="@elem.Path">@elem.Name</a></div>
        <div>@elem.Description</div>
    }
    </div>

}
@code {
    private List<Subgroup>? _model;

    protected override async Task OnInitializedAsync()
    {
        _model = await SubgroupsService.GetSubgroups(Tenant.Id);
    }
}
