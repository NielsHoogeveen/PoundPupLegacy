@using PoundPupLegacy.ViewModel.Models
@if(Model is not null) {
    <img src="@Model.FilePath" alt="@Model.Label" class="@Class"/>
}
@code {
    [Parameter]
    [EditorRequired]
    public Image? Model { get; set; }

    [Parameter]
    public string Class { get; set; } = "";
}
