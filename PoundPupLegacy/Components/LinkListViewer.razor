@using PoundPupLegacy.ViewModel.Models
@if (Links.Any())
{
    <ul>
        @foreach (var link in Links)
        {
            <LinkListElementViewer Link="@link" />
        }
    </ul>
}
@code{
    [Parameter]
    [EditorRequired]
    public required IEnumerable<BasicLink> Links { get; init; }
}