@if (Text is not null){
    @Text
}

@code {

    [Parameter]
    [EditorRequired]
    public string? Value { get; set; }

    private MarkupString? Text => Value is null ? null : (MarkupString)Value;

}
