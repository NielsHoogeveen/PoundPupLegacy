@using PoundPupLegacy.ViewModel.Models
@if (Model is not null)
{
    <NameableViewer Model="Model" />
    <DocumentableViewer Model="Model" />
}
@code{
    [Parameter]
    [EditorRequired]
    public GlobalRegion? Model { get; set; }
}

