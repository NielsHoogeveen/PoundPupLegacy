@if(Model.Any()){
    <div class="comments">
        @foreach (var comment in @Model)
        {
            <CommentViewer Model="comment" />
        }
    </div>
}
@code{
    [Parameter]
    [EditorRequired]
    public IEnumerable<Comment> Model { get; set; } = Array.Empty<Comment>();
}