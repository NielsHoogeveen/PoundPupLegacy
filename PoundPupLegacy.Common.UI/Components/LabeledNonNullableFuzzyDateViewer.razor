@using PoundPupLegacy.Common;
<div class="labeled-value labeled-fuzzy-date-value">
    <label>@Label: </label>
    @Value.ToDisplayString()
</div>

@code {
    [Parameter]
    [EditorRequired]
    public FuzzyDate Value { get; set; } = default!;

    [Parameter]
    [EditorRequired]
    public string Label { get; set; } = string.Empty;
}
