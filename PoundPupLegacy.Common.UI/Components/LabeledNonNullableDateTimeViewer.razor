<div class="labeled-value labeled-string-value">
    <label>@Label: </label>
    @Value
</div>
@code {
    [Parameter]
    [EditorRequired]
    public required DateTime Value { get; init; }

    [Parameter]
    [EditorRequired]
    public required string Label { get; init; }
}
